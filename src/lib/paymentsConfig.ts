/**
 * Mobile money (Pawapay / DusuPay) is controlled by `app_config` and the dashboard toggles.
 * Use `useMobileMoneyPayments` or `useMobileMoneyCheckoutVisible` from `../contexts/MobileMoneyPaymentContext`.
 */
